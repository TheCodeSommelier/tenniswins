<div class="c-new-bets u-c-content" data-controller="edit-bets bets">
  <%= form_for @bet, url: admin_bet_path(@bet), method: :patch do |f| %>
    <%= f.submit "Update bets", class: "c-button" %>
  <% end %>
</div>
